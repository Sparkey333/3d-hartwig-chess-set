# Client (optional — prefer server proxy credentials)
VITE_HIGGSFIELD_API_KEY=
VITE_HIGGSFIELD_API_URL=https://platform.higgsfield.ai

# AI proxy (server/ai-proxy.mjs) — never expose these to the browser
HF_CREDENTIALS=
# or:
HF_API_KEY=
HF_API_SECRET=

# Alternative providers
OPENAI_API_KEY=
REPLICATE_API_TOKEN=

AI_PROXY_PORT=8787
